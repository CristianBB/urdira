# Urdira Core MVP Phase 13 — MCP adapter evidence

Date: 2026-08-10
Branch: `phase-13-mcp`

## Delivered

- Added the official `@modelcontextprotocol/server` v2.0.0 dependency.
- Implemented the modern MCP `2026-07-28` server factory with explicit dual-era stdio serving via `serveStdio(..., { legacy: "serve" })`.
- Registered exactly four deterministic tools: `urdira_query`, `urdira_analyze_change`, `urdira_build_context`, and `urdira_index_status`.
- Derived operation argument fields from the public contract registry, normalized public inputs to canonical Urdira requests, and kept query and index-status cursor paths distinct.
- Added closed wrapper schemas, output schemas, structured/text equivalence, typed Urdira error results, MCP protocol-error separation, progress forwarding, cancellation signals, progress monotonicity, and no connection-derived workspace scope.
- Added the application-level `runUrdiraMcp` entry point and preserved daemon ownership/lifecycle through its returned close handle.

## Verification

All commands were run from the repository root on the clean-source checkout before commit:

| Check | Result |
|---|---|
| `pnpm check:architecture` | Passed for 11 workspace packages |
| `pnpm lint` | Passed |
| `pnpm test` | 36 files, 1,015 tests passed |
| `pnpm test:coverage` | Passed; repository lines 90.63% |
| `pnpm typecheck` | Passed |
| `pnpm check:coverage-gate` | Passed; critical branches 100%, semantic regions 100% |
| `git diff --check` | Passed |
| forbidden MCP transport/stdout/network scan | No forbidden implementation hits; stdout reference is test-only purity instrumentation |

Independent Luna review completed before implementation. The review specifically checked public operation mappings, public-page normalization, index-status cursor separation, typed error preservation, SDK era behavior, progress/cancellation, and scope isolation. No review files were changed.

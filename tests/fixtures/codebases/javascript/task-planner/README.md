# Task Planner (JavaScript)

This is the JavaScript counterpart of the TypeScript task-planner fixture. It
uses ESM modules, JSDoc declarations, a repository port, an in-memory adapter,
state transitions, and Node's built-in test runner.

Run it with:

```bash
npm test
```
